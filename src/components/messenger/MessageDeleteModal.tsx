import React, { ReactElement } from "react";
import ModalTemplate from "../common/ModalTemplate";

function MessageDeleteModal(): ReactElement {
  return <ModalTemplate>sd</ModalTemplate>;
}

export default MessageDeleteModal;
